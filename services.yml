apiVersion: v1
kind: Service
metadata:
  name: demo-service
  labels:
    app: movies-api
spec:
  type: LoadBalancer
  selector:
    app: movies-api
  ports:
  - protocol: TCP
    name: http
    port: 9000
    targetPort: 3000
